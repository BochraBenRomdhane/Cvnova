/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Use default optimized images on Vercel
    unoptimized: false,
  },
};

module.exports = nextConfig;
